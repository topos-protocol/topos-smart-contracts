name: Slither Analysis

on:
  pull_request:
  
jobs:
  analyze:
    name: Run the Slither static analyzer
    runs-on: ubuntu-latest-16-cores
    steps:
      - uses: actions/checkout@v3
      - uses: crytic/slither-action@v0.2.0
